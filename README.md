# Mobile Finance Demo

Static mobile-first demo site served with Vite.

## Run locally

```bash
npm install
npm run dev
```

Open the URL Vite prints. On an LXC, use the container IP with port `5173`.

## Build

```bash
npm run build
npm run preview
```
